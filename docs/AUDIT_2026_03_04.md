# Аудит проєкту — 2026-03-04

Проведено за допомогою Laravel Boost MCP.

---

## 🔴 P0 — Критичні (треба фіксити зараз)

### 1. Дублікат міграції `store_contexts` — ВСІ feature-тести зламані
- **Файли:**
  - `database/migrations/2025_01_20_000006_add_tenant_id_to_existing_tables.php` — з `hasTable` guard
  - `database/migrations/2026_01_15_183523_create_store_contexts_table.php` — **без** guard
- **Наслідок:** `RefreshDatabase` → "table already exists" → 18/18 Auth feature-тестів FAILED
- **Фікс:** Додати `if (!Schema::hasTable('store_contexts'))` guard у новішу міграцію
- **Статус:** ✅ Виправлено

### 2. Захардкоджений diagnostic secret key
- **Файл:** `app/Http/Controllers/Api/DiagnosticController.php:18`
- **Проблема:** `private string $secretKey = 'diagnostic_secret_key_2025'` — прямо в класі
- **Фікс:** Винести в `config('services.diagnostic.secret_key')` з fallback на `.env`
- **Статус:** ✅ Виправлено

### 3. Незахищені ендпоінти (потребують review)
| Роут | Проблема |
|---|---|
| `GET/PUT /widget/ai-context` | Без auth — будь-хто може читати/міняти AI контекст |
| `POST /order-status`, `/orders/search` | Без auth — перебір замовлень |
| `GET /debug/products` | Debug endpoint у production |
| `GET /analytics/debug-events` | Debug дані публічно |
| 70+ `/diagnostic/*` роутів | Тільки query-param `?key=`, без rate limiting |
- **Статус:** ⬜ Потребує окремого review

---

## 🟡 P1 — Середні (бажано фіксити)

### 4. CORS повністю відкритий
- **Файл:** `config/cors.php` — `allowed_origins: ['*']`
- **Додатково:** `app/Http/Middleware/WidgetCors.php` рефлектить origin назад з `Allow-Credentials: true` — origin reflection вразливість
- **Статус:** ⬜ Потребує окремого review

### 5. `env()` у сервіс-класах (ламає `config:cache`)
- **Файли:**
  - `app/Services/Ai/AiClient.php:18` — `env('OPENAI_API_KEY')` як fallback
  - `app/Services/Ai/EmbeddingService.php:28` — аналогічно
- **Проблема:** При `php artisan config:cache` `env()` повертає `null` → OpenAI перестає працювати
- **Фікс:** Перенести fallback у `config/services.php`
- **Статус:** ✅ Виправлено

### 6. Важкий запит без кешу в Livewire
- **Файл:** `app/Livewire/Admin/PromptPresetsManager.php:79`
- **Проблема:** `Product::distinct()->pluck('category_path')` при **кожному** рендері Livewire
- **Фікс:** Використати `Cache::remember()` на 5 хвилин
- **Статус:** ✅ Виправлено

---

## ⚪ P2 — Технічний борг

### 7. DiagnosticController.php — 6857 рядків (God Object)
- Потребує розбиття на менші контролери
- **Статус:** ⬜ Відкладено

### 8. BaseAgent.php — 3533 рядки (God Object)
- Містить prompt-generation, tool execution, product formatting, history loading, size recommendation
- Потребує декомпозиції на сервіси
- **Статус:** ⬜ Відкладено

### 9. `\Exception` замість `\Throwable` — 3 місця
- `app/Services/Agent/Tools/MeiliProductSearchTool.php:450`
- `app/Services/Agent/BaseAgent.php:2283`
- `app/Services/Agent/Tools/AiRerankTool.php:100`
- **Проблема:** Пропустить TypeError, ParseError
- **Статус:** ⬜ Відкладено

### 10. PHPUnit 12 deprecation
- `tests/Unit/IntentEnumTest.php:11` — `@dataProvider` → `#[DataProvider]`
- **Статус:** ⬜ Відкладено

### 11. 21x `withoutGlobalScope(TenantScope)` в BaseAgent
- Потенційний data leak між тенантами
- Потребує аудиту кожного виклику
- **Статус:** ⬜ Відкладено
