{
    "servers": {
        "laravel-boost": {
            "type": "stdio",
            "command": "/usr/bin/php8.3",
            "args": [
                "artisan",
                "boost:mcp"
            ],
            "cwd": "${workspaceFolder}"
        }
    }
}